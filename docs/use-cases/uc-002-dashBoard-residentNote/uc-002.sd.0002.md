# UC-002 Dashboard ResidentNote Sequence Diagram

## Metadata
| Key               | Value                             |
|-------------------|-----------------------------------|
| Id                | SD-UC-002                         |
| crossReference    | SSD-UC-002 / OC-UC-002            |

## Version Log
| Version | Date       | Description              | Author     |
|---------|------------|--------------------------|------------|
| 0003    | 2026-06-07 | Update: use WebApi for CRUD | Team 6     |

## Sequence Diagram
```mermaid
sequenceDiagram
    actor Employee as Actor
    participant WebUI as WebUI
    participant ResidentNoteController as ResidentNoteController
    participant ResidentNoteService as ResidentNoteService
    participant WebApi as WebApi
    participant ResidentNoteRepository as ResidentNoteRepository

    Employee->>+WebUI: selectResident(residentId)
    WebUI->>+ResidentNoteController: getResidentNotes(residentId)
    ResidentNoteController->>+ResidentNoteService: getResidentNotesForResident(residentId)
    ResidentNoteService->>+WebApi: GET /residentNotes?residentId={residentId}
    WebApi->>+ResidentNoteRepository: fetchResidentNotes(residentId)
    ResidentNoteRepository-->>-WebApi: ResidentNotes
    WebApi-->>-ResidentNoteService: ResidentNotes
    ResidentNoteService-->>-ResidentNoteController: ResidentNotes
    ResidentNoteController-->>-WebUI: ResidentNotes
    WebUI-->>-Employee: showResidentNotes(ResidentNotes)

    Employee->>+WebUI: addResidentNote(residentId, ResidentNoteText)
    WebUI->>+ResidentNoteController: addResidentNote(residentId, ResidentNoteText)
    ResidentNoteController->>+ResidentNoteService: addResidentNote(residentId, ResidentNoteText)
    ResidentNoteService->>+WebApi: POST /residentNotes (AddResidentNoteDto)
    WebApi->>+ResidentNoteRepository: saveResidentNote(residentId, noteText)
    alt addResidentNote success
        ResidentNoteRepository-->>WebApi: ResidentNoteSaved
        WebApi-->>ResidentNoteService: ResidentNoteSaved
        ResidentNoteService-->>ResidentNoteController: success
        ResidentNoteController-->>WebUI: confirmResidentNoteAdded()
        WebUI-->>Employee: confirmResidentNoteAdded()
    else addResidentNote error
        ResidentNoteRepository-->>-WebApi: error
        WebApi-->>-ResidentNoteService: error
        ResidentNoteService-->>-ResidentNoteController: error
        ResidentNoteController-->>-WebUI: errorResidentNote("ResidentNote cannot be saved")
        WebUI-->>-Employee: errorResidentNote("ResidentNote cannot be saved")
    end

    Employee->>+WebUI: editResidentNote(residentId, residentNoteId, newText)
    WebUI->>+ResidentNoteController: editResidentNote(residentId, residentNoteId, newText)
    ResidentNoteController->>+ResidentNoteService: editResidentNote(residentId, residentNoteId, newText)
    ResidentNoteService->>+WebApi: PUT /residentNotes/{residentNoteId} (EditResidentNoteDto)
    WebApi->>+ResidentNoteRepository: updateResidentNote(residentId, residentNoteId, newText)
    alt editResidentNote success
        ResidentNoteRepository-->>WebApi: ResidentNoteUpdated
        WebApi-->>ResidentNoteService: ResidentNoteUpdated
        ResidentNoteService-->>ResidentNoteController: success
        ResidentNoteController-->>WebUI: confirmResidentNoteEdited()
        WebUI-->>Employee: confirmResidentNoteEdited()
    else editResidentNote error
        ResidentNoteRepository-->>-WebApi: error
        WebApi-->>-ResidentNoteService: error
        ResidentNoteService-->>-ResidentNoteController: error
        ResidentNoteController-->>-WebUI: errorResidentNote("ResidentNote cannot be saved")
        WebUI-->>-Employee: errorResidentNote("ResidentNote cannot be saved")
    end

    Employee->>+WebUI: deleteResidentNote(residentId, residentNoteId)
    WebUI->>+ResidentNoteController: deleteResidentNote(residentId, residentNoteId)
    ResidentNoteController->>+ResidentNoteService: deleteResidentNote(residentId, residentNoteId)
    ResidentNoteService->>+WebApi: DELETE /residentNotes/{residentNoteId}
    WebApi->>+ResidentNoteRepository: deleteResidentNote(residentId, residentNoteId)
    alt deleteResidentNote success
        ResidentNoteRepository-->>WebApi: ResidentNoteDeleted
        WebApi-->>ResidentNoteService: ResidentNoteDeleted
        ResidentNoteService-->>ResidentNoteController: success
        ResidentNoteController-->>WebUI: confirmResidentNoteDeleted()
        WebUI-->>Employee: confirmResidentNoteDeleted()
    else deleteResidentNote error
        ResidentNoteRepository-->>-WebApi: error
        WebApi-->>-ResidentNoteService: error
        ResidentNoteService-->>-ResidentNoteController: error
        ResidentNoteController-->>-WebUI: errorResidentNote("ResidentNote cannot be deleted")
        WebUI-->>-Employee: errorResidentNote("ResidentNote cannot be deleted")
    end


```

**Note:** While Strict UML purists argue that actor is not part of sequence diagram, we can use actor in sequence diagram if it helps to clarify the interactions and roles of different participants in the system. The key is to ensure that the diagram remains clear and easy to understand for all stakeholders even it breaks strict UML rules.

---

**Note on DTOs and Data Transformation:**
- Data Transfer Objects (DTOs) are required between WebUI and WebApi to decouple UI models from domain models.
- Example: `addResidentNote(residentId, ResidentNoteText)` in WebUI is transformed into an `AddResidentNoteDto` when sent to the WebApi.
- Data returned from the database is mapped to DTOs before being sent to the WebUI.
- All data transformations should be explicit and documented in the implementation.

**DTO Example:**
```csharp
public class AddResidentNoteDto
{
    public int residentId { get; set; }
    public string ResidentNote { get; set; }
}
```
