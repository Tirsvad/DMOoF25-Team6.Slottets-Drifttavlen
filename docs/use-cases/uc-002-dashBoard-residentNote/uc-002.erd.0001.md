# Entity Relationship Diagram (ERD) for UC-002 Dashboard ResidentNote
## Metadata
| Key               | Value                             |
|-------------------|-----------------------------------|
| Id                | ERD-002                        |
| crossReference    | DCD-002                        |

## Version Log
| Version | Date       | Description                        | Author     |
|---------|------------|------------------------------------|------------|
| 0001    | 2026-03-06 | Initial                            | Team 6     |

## Entity Relationship Diagram
```mermaid
erDiagram
    Resident {
        int ResidentId
        string Name
    }
    ResidentNote {
        int ResidentNoteId
        int ResidentId
        string NoteText
        DateTime CreatedAt
        DateTime EditedAt
    }
    AddResidentNoteDto {
        int residentId
        string ResidentNote
    }
    EditResidentNoteDto {
        int residentId
        int residentNoteId
        string newText
    }
    ResidentNoteService {
        getResidentNotesForResident(residentId): List<ResidentNote>
        addResidentNote(dto: AddResidentNoteDto): bool
        editResidentNote(dto: EditResidentNoteDto): bool
        deleteResidentNote(residentId, residentNoteId): bool
    }
    ResidentNoteController {
        getResidentNotes(residentId): List<ResidentNote>
        addResidentNote(residentId, noteText): bool
        editResidentNote(residentId, residentNoteId, newText): bool
        deleteResidentNote(residentId, residentNoteId): bool
    }
    IResidentNoteRepository {
        manages ResidentNote
    }
    Resident ||--o{ ResidentNote : has
    ResidentNoteController ..> ResidentNoteService : uses
    ResidentNoteService ..> AddResidentNoteDto : uses
    ResidentNoteService ..> EditResidentNoteDto : uses
    ResidentNoteService ..> ResidentNoteController : depends
    ResidentNoteController ..> IResidentNoteRepository : depends
    IResidentNoteRepository }o--|| ResidentNote : manages
```

**Notes:**
- All placeholders have been replaced with UC-002 specific content.
- Entities, attributes, and relationships are clearly defined.
- See DCD for domain class details.
- Only the latest version is kept in the main branch.
