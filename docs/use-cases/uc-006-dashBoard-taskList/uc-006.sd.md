# Sequence Diagram: TaskList Management

## Metadata
| Key            | Value           |
|----------------|-----------------|
| Id             | UC-006.SD       |
| crossReference | UC-006.OC       |

## Version Log
| Version | Date       | Description | Author |
|---------|------------|-------------|--------|
| 0001    | 2026-04-10 | Initial     | Team 6 |

## Presentation Layer → Application Layer
```mermaid
sequenceDiagram
    actor Caregiver
    participant DashboardUI
    participant TaskService
    Caregiver ->> DashboardUI: OpenDashboardView()
    DashboardUI ->> TaskService: GetAvailableTasks(shiftId)
    TaskService -->> DashboardUI: AvailableTasksDTO
    DashboardUI -->> Caregiver: ShowTaskList(tasks)
    Caregiver ->> DashboardUI: UpdateTaskStatus(taskId, status)
    DashboardUI ->> TaskService: UpdateTaskStatus(taskId, status)
    TaskService -->> DashboardUI: TaskStatusUpdatedConfirmation
    DashboardUI -->> Caregiver: ShowTaskStatusUpdated(taskId, status)
    Caregiver ->> DashboardUI: MarkTaskComplete(taskId)
    DashboardUI ->> TaskService: MarkTaskComplete(taskId)
    TaskService -->> DashboardUI: TaskCompletedConfirmation
    DashboardUI -->> Caregiver: ShowTaskCompleted(taskId)
```

## Application Layer → Infrastructure Layer (Data Access)
```mermaid
sequenceDiagram
    participant TaskService
    participant TaskRepository
    TaskService ->> TaskRepository: GetAvailableTasks(shiftId)
    TaskRepository -->> TaskService: AvailableTasksDTO
    TaskService ->> TaskRepository: UpdateTaskStatus(taskId, status)
    TaskRepository -->> TaskService: TaskStatusUpdatedConfirmation
    TaskService ->> TaskRepository: MarkTaskComplete(taskId)
    TaskRepository -->> TaskService: TaskCompletedConfirmation
```

## Notes
- DTOs are used for data transfer between layers.
- No direct database or internal system calls are shown; all data access is abstracted via TaskRepository.
- All method names use PascalCase and parameters are shown in parentheses.
